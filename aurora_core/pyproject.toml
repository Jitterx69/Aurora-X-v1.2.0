[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "aurora_core"
version = "0.1.0"
description = "AURORA-X high-performance compute core (Rust)"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
